# Ruta base que usa el cliente HTTP. Se deja en /api para pasar por el proxy del dev
# server y evitar CORS contra el host de origen.
VITE_API_BASE_URL=/api

# Host real al que el proxy de Vite reenvía las peticiones /api/*
VITE_API_TARGET=https://ebind-dev.egl-cloud.com
